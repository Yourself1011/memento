{
  "name": "server",
  "version": "1.0.0",
  "description": "The backend for Memento, created with Express and MongoDB/Mongoose.",
  "license": "BSD-3 Clause",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.1.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
